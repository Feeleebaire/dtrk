<div class="container">
  <h1><%= @plan.name %></h1>
  <table class="table">
      <% @publishers.each do |publisher| %>
      <tr>
        <td><%= publisher.name %></td>
        <td><%= publisher.type.name%></td>
       </tr>
       <p></p>
      <% end %>
  </table>
</div>

